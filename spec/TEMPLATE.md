# OpenAPI &lt;Name&gt; Extension

**Spec version:** 0.1.0

---

## 1. Introduction

Short description of the problem this extension solves and the OpenAPI field(s) it adds (e.g. `x-<name>`).

## 2. Overview

```yaml
components:
  <extensionField>:
    ...
```

## 3. Conventions

The key words "MUST", "MUST NOT", "REQUIRED", "SHOULD", "MAY" are to be interpreted as described in [RFC 2119](https://www.rfc-editor.org/rfc/rfc2119).

## 4. Object Definitions

Define the objects, fields, and types the extension introduces.

## 5. Applying via OpenAPI Overlays

Show how the extension can be applied to an existing document without modification, using an [OpenAPI Overlay](https://spec.openapis.org/overlay/v1.0.0.html).

## 6. Examples

## 7. Validation

List the rules a conforming implementation MUST enforce.

## Reference Implementation

Link to any reference implementation(s), if available.
